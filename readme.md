src/
├── config/
│   └── urls.js            (was data/url.js) // URLs 
├── payloads/
│   ├── authPayload.js     (was data/requestBody.js) // Request Body 
│   └── userData.js        (was data/userData.js) // User Data 
├── services/
│   └── apiService.js      (was core/api.js) // Writing the API End Points 
├── tests/
│   └── generateToken.js   (was test/generateToken.js) // Generating Token
└── utils/
    ├── crypto.js          (was utils/generatAndCreateHash.js) // Generating Hashes
    └── httpClient.js      (was utils/helpers.js) // HTTP Client 


This helps the Folder structure to be more readable and maintainable.
